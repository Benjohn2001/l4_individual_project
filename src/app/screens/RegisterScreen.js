import React from 'react';
import { Text, View, Image } from 'react-native';

const RegisterScreen = ({ navigation }) => {
    return (
        <View className='flex-1 items-center justify-center bg-primaryPurple'>
                <Text>Register</Text>
        </View>
    );
}
export default RegisterScreen;